using System;

public static class GameConstants
{
    public const int SmallScore = 100;
    public const int BigScore = 300;
    public static TimeSpan SmallTime = new(0, 0, 5);
    public static TimeSpan BigTime = new(0, 0, 10);
}